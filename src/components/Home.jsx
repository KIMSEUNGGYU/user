import styled from '@emotion/styled';

const Home = () => {
  return (
    <Wrapper>
      <h1>Home Component</h1>
    </Wrapper>
  );
};

const Wrapper = styled.div`
  display: flex;
  justify-content: center;
`;

export default Home;
